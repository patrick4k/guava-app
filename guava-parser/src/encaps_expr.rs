use crate::Capture;
use crate::ast::*;
use crate::FromCapture;
use crate::Rule;

impl FromCapture<'_> for EncapsExpr {
    fn from_cap<'a>(cap: Capture<'a>) -> Self {
        todo!("IMPL ENCAPS EXPR")
    }
}
